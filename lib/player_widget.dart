
import 'package:flutter/material.dart';

class PlayerWidget extends StatelessWidget {
  const PlayerWidget({Key? key}) : super(key: key);

  _onPressed() {
    print('Player');
  }

  @override
  Widget build(BuildContext context) {
    return Text('Mac McClung');
  }
}